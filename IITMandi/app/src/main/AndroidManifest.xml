<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.priyanshu.iitmandi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        //MainActivity
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name=".DemoActivity"
            android:label="Faculty"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ClubsActivity"
            android:label="Clubs"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ClubDetailActivity"
            android:label="@string/title_activity_club_detail"></activity>
        <activity
            android:name=".OverviewActivity"
            android:label="Overview"></activity>
    </application>

</manifest>